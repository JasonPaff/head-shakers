import type { Metadata } from 'next';

export default function ExportSettingsPage() {
  return <div>Export Settings Page</div>;
}

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'Export Settings',
  };
}
