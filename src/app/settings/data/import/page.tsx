import type { Metadata } from 'next';

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'Import Settings',
  };
}

export default function ImportSettingsPage() {
  return <div>Import Settings Page</div>;
}
