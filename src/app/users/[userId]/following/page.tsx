import type { Metadata } from 'next';

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'User Following',
  };
}

export default function UserFollowingPage() {
  return <div>User Following Page</div>;
}
