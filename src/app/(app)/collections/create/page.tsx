import type { Metadata } from 'next';

export default function CreateCollectionPage() {
  return <div>Create Collection Page</div>;
}

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'Create Collection',
  };
}
