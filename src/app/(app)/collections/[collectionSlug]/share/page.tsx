import type { Metadata } from 'next';

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'Share Collection',
  };
}

export default function ShareCollectionPage() {
  return <div>Share Collection Page</div>;
}
