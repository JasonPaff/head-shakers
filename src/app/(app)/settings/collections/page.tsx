import type { Metadata } from 'next';

export default function CollectionSettingsPage() {
  return <div>Collection Settings Page</div>;
}

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'Collection Settings',
  };
}
