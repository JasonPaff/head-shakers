import type { Metadata } from 'next';

export default function DashboardCollectionAnalyticsPage() {
  return <div>Dashboard Collection Analytics Page</div>;
}

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'Collection Analytics',
  };
}
