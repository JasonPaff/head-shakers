import type { Metadata } from 'next';

export default function DashboardNotificationsPage() {
  return <div>Dashboard Notifications Page</div>;
}

export function generateMetadata(): Metadata {
  return {
    description: '',
    title: 'Dashboard Notifications',
  };
}
