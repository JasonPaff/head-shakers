import { BaseContextHelpers } from '@/lib/queries/base/base-context-helpers';

/**
 * Abstract base class for all facade operations
 * Extends BaseContextHelpers for query context utilities
 *
 * Future: Add common facade utilities (error handling, caching, etc.)
 */
export abstract class BaseFacade extends BaseContextHelpers {
  // Minimal for now - can add facade-specific utilities later
}
