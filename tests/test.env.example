# Test Environment Configuration

# AUTOMATIC CONTAINERS (Recommended - No config needed!)
# Just ensure Docker Desktop is running and leave DATABASE_URL_TEST unset
# The test setup will automatically start a PostgreSQL container

# MANUAL DATABASE OPTIONS (If Docker issues occur)
# Uncomment ONE of these options:

# Option 1: Manual Docker container
# DATABASE_URL_TEST=postgresql://test_user:test_password@localhost:5433/headshakers_test

# Option 2: Neon test branch
# DATABASE_URL_TEST=postgresql://user:pass@host/db_test

# Test configuration
NODE_ENV=test
SKIP_ENV_VALIDATION=true

# Mock external services in tests
CLERK_SECRET_KEY=test_clerk_secret
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=test_clerk_pk
CLOUDINARY_CLOUD_NAME=test_cloudinary
CLOUDINARY_API_KEY=test_api_key
CLOUDINARY_API_SECRET=test_api_secret
ABLY_API_KEY=test_ably_key
REDIS_URL=redis://localhost:6379/1
RESEND_API_KEY=test_resend_key