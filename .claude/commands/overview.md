---
allowed-tools: Bash(ls, find)
---

# Project Overview: $ARGUMENTS

## Project Structure
!`find . -type f -name "*.tsx" -o -name "*.ts" -o -name "*.js" | head -20`

## Project Configuration
@package.json

Based on the file structure above and package.json, provide an overview focusing on: $ARGUMENTS

Include:
1. What this application does
2. Key technologies and file types found
3. How $ARGUMENTS relates to the actual files in this project
4. Specific files that seem relevant to $ARGUMENTS

Keep it practical and specific.